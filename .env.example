# ========================================
# MiniRAG-Local 环境配置示例
# ========================================
# 复制此文件为 .env 并修改相应配置
# 使用方法: cp .env.example .env

# ========================================
# AgentAPI 配置
# ========================================

# AgentAPI 服务地址
AGENT_API_URL=http://localhost:61000

# AgentAPI 服务端口
AGENT_API_PORT=61000

# ========================================
# OpenAI Proxy 配置
# ========================================

# OpenAI 兼容层端口
PORT=62000


# 本地知识库目录（你的 Markdown 笔记或文档目录）
# 示例:
#   - macOS: /Users/yourname/Documents
#   - Linux: /home/yourname/Documents
#   - Obsidian: /Users/yourname/Obsidian/MyVault
WORKSPACE_PATH=/path/to/your/knowledge-base

# ========================================
# AgentAPI 二进制文件路径
# ========================================

# AgentAPI 可执行文件完整路径
# 从 https://github.com/coder/agentapi 克隆并编译
# 编译命令: CGO_ENABLED=0 go build -o out/agentapi main.go
# 示例: /Users/yourname/.openclaw/agentapi/out/agentapi
AGENT_API_BINARY=/path/to/your/agentapi/out/agentapi

# ========================================
# AI Agent 配置
# ========================================

# Claude CLI 路径（如果使用 Claude）
# 安装: brew install claude
# 示例: /opt/homebrew/bin/claude 或 /usr/local/bin/claude
CLAUDE_BINARY=/opt/homebrew/bin/claude

# Gemini CLI 路径（如果使用 Gemini）
# 安装: npm install -g @google/gemini-cli
# 示例: /opt/homebrew/bin/gemini 或 /usr/local/bin/gemini
GEMINI_BINARY=/opt/homebrew/bin/gemini

# 默认使用的 Agent（可选值: claude 或 gemini）
# 启动时可以通过参数覆盖: ./scripts/start_ai.sh gemini
DEFAULT_AGENT=claude

# Claude CLI 额外参数（传递给 claude 命令）
# 默认: --dangerously-skip-permissions
# 可添加: --model claude-opus-4-5 等
CLAUDE_ARGS=--dangerously-skip-permissions

# Gemini CLI 额外参数（传递给 gemini 命令）
# 留空则使用脚本默认值: --yolo --include-directories $WORKSPACE_PATH
# 如需自定义，可填写完整参数，例如: --yolo --include-directories /custom/path
GEMINI_ARGS=

# ========================================
# DingTalk 独立机器人配置（可选）
# 不依赖 OpenClaw，直接对接钉钉群
# ========================================

# 钉钉应用 AppKey（企业内部应用 → 凭证与基础信息）
DINGTALK_APP_KEY=

# 钉钉应用 AppSecret
DINGTALK_APP_SECRET=

# RAG 群组路由配置（JSON 格式）
# key: 钉钉群的 conversationId（从机器人收到消息的日志中获取）
# value: { endpoint, model, timeoutMs }
DINGTALK_RAG_GROUPS={"cidXXXXXX": {"endpoint": "http://localhost:62000/v1/chat/completions", "model": "rag", "timeoutMs": 30000}}

# ========================================
# 启动提示词配置
# ========================================

# agentapi 就绪后自动发送一次给 AI Agent，用于规范行为
# 留空则不发送
# 示例: 你是一个知识库助手，请保持回答简洁准确，不超过500字
STARTUP_PROMPT=

